# log-insight-service
A Spring Boot (or Node) service that takes application logs as input and uses an LLM to:  Summarize what happened,  Highlight errors &amp; root-cause candidates,  Suggest next debugging steps.
